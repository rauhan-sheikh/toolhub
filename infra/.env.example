# Contact address for the ACME account (Let's Encrypt / ZeroSSL).
#
# Use a real address you read: it receives certificate-expiry warnings, and
# ZeroSSL — Caddy's fallback issuer — rejects obvious placeholders outright.
ACME_EMAIL=you@yourdomain.com
